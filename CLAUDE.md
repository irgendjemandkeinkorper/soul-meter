# Soul Meter — Claude entry point

@AGENTS.md

`AGENTS.md` contains the shared repository rules for every agent. They apply only to Soul Meter. Read its task-specific pointers rather than loading the entire documentation set at startup.

For planning and assigned handoffs, follow `/home/adamjroder/AI-ROLE-POLICY.md`. Preserve the user's authorized scope, ratified game design, and explicit acceptance criteria. Cross-model review uses `/octo` only when the user requests it.

Historical implementation progress is in `docs/status/2026-09-12-inherited-project-status.md`; verify changing claims against current code. Agent-owned runtime verification is in `docs/agent-verification.md`.
