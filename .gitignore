# Godot 4+ specific ignores
.godot/
/android/

# gdUnit4 test run output (regenerated every run, see docs/testing.md)
/reports/

# Claude Octopus session state (local tool scratch)
.claude-octopus/

# Claude Code worktree sessions (local tool scratch, not repo content)
.claude/worktrees/
build/

# Claude Code local scratch (handoffs, debug logs, worker notes)
.ai-work/

# Runtime evidence written by the combat lab and dev console when they run. HANDOFF-A1.md
# records the convention that these stay untracked; 22 blank session records reached the repo
# anyway, and a test run adds more every time.
combat_lab_recorder/
dev-console-recorder/
